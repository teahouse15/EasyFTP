package myjinji;

/**
 * 连接的即时变化
 * @author bogendihong
 */
public class Setting {

    // 0为binary 1为ascii
    static int transMode = 1;

    // 0=false;1=true
    static int verbose = 1; // 详细模式，默认打开
}
